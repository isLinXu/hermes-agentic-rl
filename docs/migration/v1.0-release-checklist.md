# v1.0.0 Release Checklist

> **Project:** hermes-agentic-rl  
> **Target Version:** `1.0.0-rc1` → `1.0.0`  
> **Date:** 2026-07-08

---

## Code Quality

- [x] **ruff** passes with 0 errors on all modified files.
- [x] **mypy** passes with 0 errors on all modified files ( `--follow-imports=skip` ).
- [x] No new lint warnings introduced by the stability-marker changes.
- [x] `@stable` decorators are placed correctly on all frozen public symbols.

## Test Coverage

- [x] `pytest` collects **1,017 tests**.
- [x] Core test suite passes (`pytest tests/ -q`).
- [x] No test failures related to the `@stable` decorator additions.

## End-to-End Validation

- [x] **7 smoke cases** pass (MVP end-to-end, online cycle, backend compat, runtime adapter, etc.).
- [x] `python -m hermes_agentic_rl.cli.main --version` outputs `1.0.0-rc1`.
- [x] Trainer `train()` loop completes without regression on `LetterCountingEnv` + `TinyBackend`.

## Documentation

- [x] **Sphinx API docs** cover 15+ module files.
- [x] `docs/migration/v0.12-to-v1.0.md` — migration guide complete.
- [x] `docs/migration/v1.0-release-checklist.md` — this checklist complete.
- [x] `_compat.py` `__stable_apis__` registry synchronized with `@stable` decorators.
- [x] `ROADMAP.md` updated to reflect v1.0 milestone.

## Docker & Deployment

- [x] **Multi-stage build** (`Dockerfile` with builder + runtime stages).
- [x] `HEALTHCHECK` instruction present in `Dockerfile`.
- [x] **Non-root user** (`USER hermes`) in final stage.
- [x] Image builds and runs without error.

## CI / CD Pipeline

- [x] **lint** stage passes (ruff + mypy).
- [x] **test** stage passes (pytest suite).
- [x] **benchmark** stage passes (performance regression check).
- [x] **docs** stage passes (Sphinx build + link check).
- [x] All stages run in correct order with no skipped jobs.

## Core API Freeze

- [x] `hermes_agentic_rl/core/types.py` — `@stable` on `RolloutStep`, `Trajectory`, `RewardResult`, `RewardSummary`, `TrainSample`, `dataclass_to_dict`.
- [x] `hermes_agentic_rl/algos/base.py` — `@stable` on `BaseAlgo`, `RolloutRecord`, `RolloutBatch`, `AlgoUpdateStats`, `stack_cached_logprobs`, `old_logprobs_tensor`.
- [x] `hermes_agentic_rl/backends/base.py` — `@stable` on `LLMBackend`, `TokenizerProtocol`, `GenerationOutput`, `BackendUnavailableError`.
- [x] `hermes_agentic_rl/trainers/on_policy.py` — `@stable` on `OnPolicyTrainer.__init__` and `OnPolicyTrainer.train`.
- [x] `hermes_agentic_rl/rewards/base.py` — `@stable` on `BaseReward`.

## Backward-Compatibility Promise

- [x] Deprecation policy documented in `docs/migration/v0.12-to-v1.0.md`.
- [x] `@stable` decorator semantics documented in `_compat.py` docstrings.
- [x] `__stable_apis__` registry updated in `_compat.py`.
- [x] No `@stable` symbol was removed or changed in signature during this release.

## Version & Packaging

- [x] `hermes_agentic_rl/__init__.py` version bumped to `1.0.0-rc1`.
- [x] `pyproject.toml` / `setup.py` version aligned (if applicable).
- [x] `CHANGELOG.md` / release notes drafted with v1.0.0 highlights.

## Community & Announcements

- [ ] Final CHANGELOG version notes published.
- [ ] GitHub Release draft created with migration guide link.
- [ ] Community announcement posted (Discord / Slack / mailing list).
- [ ] PyPI upload prepared (`python -m build && twine check dist/*`).

---

## Sign-Off

| Role | Name | Date | Signature |
|------|------|------|-----------|
| Release Manager | — | 2026-07-08 | — |
| QA Lead | — | 2026-07-08 | — |
| Docs Lead | — | 2026-07-08 | — |

---

*Once all boxes are checked, tag `v1.0.0` and publish.*
