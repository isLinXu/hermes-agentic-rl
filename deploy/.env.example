# 环境变量示例
# 复制为 .env 并在实际部署前填入真实值
#
# cp .env.example .env

# ============================================================================
# Weights & Biases（训练指标追踪）
# ============================================================================
WANDB_API_KEY=

# ============================================================================
# API 密钥（online cycle / 推理使用）
# ============================================================================
# NewAPI / OpenAI 兼容端点
NEWAPI_API_KEY=

# OpenAI 官方 API（备用）
OPENAI_API_KEY=

# LKEAP API（备用）
LKEAP_API_KEY=

# ============================================================================
# Hermes 运行时配置
# ============================================================================
# Hermes-agent 仓库路径（默认使用子模块）
HERMES_AGENT_REPO=/opt/hermes-agentic-rl/subprojects/hermes-agent

# ============================================================================
# PyTorch 配置
# ============================================================================
# Apple Silicon MPS 回退（仅限本地 Mac 开发）
PYTORCH_ENABLE_MPS_FALLBACK=0

# ============================================================================
# 训练配置
# ============================================================================
# 默认训练配置文件
HERMES_RL_DEFAULT_CONFIG=configs/echo_grpo_mvp.yaml

# 输出目录
HERMES_RL_OUTPUT_DIR=/opt/hermes-agentic-rl/outputs

# 日志级别: DEBUG, INFO, WARNING, ERROR
HERMES_RL_LOG_LEVEL=INFO

# 指标格式: jsonl, stdout, tensorboard, wandb
HERMES_RL_METRICS_FORMAT=jsonl
