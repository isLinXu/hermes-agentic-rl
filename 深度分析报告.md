# hermes-agentic-rl 深度分析报告

> 生成时间：2026-05-19  
> 项目版本：`0.6.0.dev0`（pyproject.toml）  
> 仓库尺度：`hermes_agentic_rl/` 共 **135 个 Python 模块** + **29 份 YAML 配置** + **39 份文档**  
> 分析方法：源码逐层穿读（CLI → Trainer → Algo → Reward → Runtime → Eval）+ 横向算法对比

---

## 目录

1. [项目定位与产品决策](#1-项目定位与产品决策)
2. [整体架构](#2-整体架构)
3. [数据契约（核心 dataclass）](#3-数据契约核心-dataclass)
4. [算法层 cheat sheet](#4-算法层-cheat-sheet)
5. [Trainer 工程特性矩阵](#5-trainer-工程特性矩阵)
6. [奖励系统](#6-奖励系统)
7. [Hermes 运行时集成](#7-hermes-运行时集成)
8. [评估系统与促晋闸门](#8-评估系统与促晋闸门)
9. [Online Cycle 闭环](#9-online-cycle-闭环)
10. [配置体系（29 份 YAML）](#10-配置体系29-份-yaml)
11. [可观测性栈](#11-可观测性栈)
12. [横向对比（vs verl / TRL / OpenRLHF）](#12-横向对比vs-verl--trl--openrlhf)
13. [工程基线与质量门](#13-工程基线与质量门)
14. [技术债务清单](#14-技术债务清单)
15. [改进建议（按优先级）](#15-改进建议按优先级)
16. [复用价值评估（给读者）](#16-复用价值评估给读者)
17. [一图速览 & 结论](#17-一图速览--结论)

---

## 1. 项目定位与产品决策

### 1.1 一句话定位

> `hermes-agentic-rl` 是围绕 **Hermes-agent**（执行层）构建的 **RL 学习层**，把 Hermes 真实运行轨迹转成 _可训练 / 可评估 / 可促晋_ 的反馈闭环。它**不是**通用 LLM 微调框架。

### 1.2 五个关键产品决策

| 决策 | 含义 | 体现在哪 |
|---|---|---|
| **不做通用微调** | 只针对 Hermes 风格 agent 行为分布做 RL | reward / env / 评估都围绕 tool-call 结构、terminal 命令、多轮回复 |
| **双路径架构** | `train-rl`（本地 GRPO/PPO）+ `online-cycle`（真实 Hermes + sidecar + worker） | `cli/train_rl.py`、`cli/online_cycle_cli.py` 两条独立流水线 |
| **基准先行** | 不信训练 reward，信 held-out 评估 + 配对 A/B | `eval/rl_eval.py`、`eval-gate` 退出码 `3` 阻断升迁 |
| **trainable surface 显式枚举** | 明确说明哪些参数会被更新 | policy backbone / value head / LoRA / RM head / BC/DPO worker |
| **远端模型不训权重** | OpenAI-compatible 模型只能采集数据 | online-cycle 训练的是 sidecar worker，**远端 LLM 权重不动** |

### 1.3 三个反差点（容易误解之处）

| 误解 | 真相 |
|---|---|
| "online-cycle 在线训练远端模型" | ❌ 用远端模型采集数据，本地训 worker；远端权重永不更新 |
| "默认 `tiny` backend 能学出能力" | ❌ Tiny 是 **30K 参数 char-level transformer**，仅供闭环 smoke；要真训能力需切到 `hf` + LoRA |
| "GRPO 的通用实现" | ❌ 它是 Hermes 行为分布优化器，reward 默认偏向 tool-call 结构正确性而非内容相似 |

---

## 2. 整体架构

### 2.1 包结构（实测 135 个 .py 模块）

```
hermes_agentic_rl/                                     135 modules
├── cli/             8    rollout / train / train-rl / eval-rl / eval-gate /
│                         online-cycle / session-* / self-evolution-batch / preflight
├── core/            7    Trajectory / RolloutStep / RewardResult / TrainSample /
│                         RolloutManager / RewardManager / TrainerBridge / Registry
├── algos/          18    7 种算法 + 7 个 common 工具：
│                         GRPO / PPO / RLOO / OPD / Hybrid / Factored / OPD-TopK
│                         common/{advantage, gae, kl, loss, reinforce_pp, temperature}
│                         entropy_schedule / best_of_n
├── trainers/       14    OnPolicyTrainer (skeleton ~1800 行) → GRPO/PPOTrainer +
│                         checkpoint / lr_schedule / mixed_precision /
│                         multi_turn_credit / token_budget / interleaved
├── backends/        6    LLMBackend protocol + tiny / hf / vLLM-rollout / batch_generate
├── runtime/         7    fake / hermes adapter + hermes_wrapper + 多入口探测
├── envs/           12    echo / sim_tool / letter_counting / curriculum / code_fix /
│                         terminal / hermes_reasoning_traces / multi_agent / sandbox
├── rewards/        12    outcome / toolcall / fs_verifier / next_turn_feedback /
│                         PRM / NextStatePRM / RewardModel / lagrangian / shaping
├── eval/            6    rl_eval / capability_axes / harness / ab_test / version_manager
├── offline/         5    BC / DPO / replay_buffer / per_buffer
├── collectors/      8    sidecar / replay_export / replay_quality / session_judge /
│                         preference_mining / conversation_collector
├── exporters/       3    self-evolution / atropos jsonl
├── monitor/         3    JSONL / TensorBoard / W&B / live dashboard (stdlib HTTP)
├── distributed/     3    MPRolloutPool（mp 队列）+ RayRolloutPool（可选）
├── peft/            2    LoRA injection (target_patterns + merge_into_base)
├── mdp/             4    PromptStateEncoder / observation / action_space
├── integrations/    6    atropos / hermes preflight + repo 路径解析
├── agent_loop/      4    PolicyAgentLoop（单轮）/ MultiTurnAgentLoop（含 <tool_call> 协议）
├── datasets/        3    JSONL / HF parquet 加载器
└── framework/       2    EnvTrainingPipeline / SessionTrainingPipeline
```

### 2.2 分层视图

```
┌─────────────────────────────────────────────────────────────┐
│ CLI 层  (cli/)                                               │
│  main.py + train_rl.py + online_cycle_cli.py + ...           │
└─────────────┬───────────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────────────────────────┐
│ Trainer 层  (trainers/)                                      │
│  OnPolicyTrainer  ◀── 共享 rollout→loss→step skeleton        │
│  GRPOTrainer / PPOTrainer  ◀── 薄包装                        │
│  CheckpointManager  ◀── {model, optim, rng, stats, kl, rms}  │
└───────┬──────────────────────────────────────────────┬──────┘
        ↓                                              ↓
┌───────────────┐  ┌───────────────────┐  ┌──────────────────┐
│ Algo (algos/) │  │ Rewards (rewards/)│  │ Backend          │
│  GRPO         │  │  RewardManager    │  │  LLMBackend 协议 │
│  PPO + GAE    │  │  多组件加权       │  │  tiny / hf       │
│  RLOO         │  │  PRM/NextStatePRM │  │  value head      │
│  OPD          │  │  RewardModel head │  │  LoRA inject     │
│  Hybrid       │  │  Lagrangian       │  │  vLLM rollout    │
│  Factored     │  └───────────────────┘  └──────────────────┘
└───────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│ Env + MDP + Agent Loop                                       │
│  BaseEnv 子类（含 CurriculumEnv 自动晋级）                   │
│  PolicyAgentLoop / MultiTurnAgentLoop（<tool_call> 协议）    │
│  PromptStateEncoder（tokenizer-agnostic）                    │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 四个核心架构原则

1. **算法是纯函数**  
   `BaseAlgo.compute_loss(policy, ref, batch) → (loss, AlgoUpdateStats)`，不持有 optimizer，可单元测试，可热替换。
2. **Backend 用 protocol，不用继承**  
   任何实现 `generate / score / score_batch / trainable_parameters` 的对象都是合法 policy（见 `backends/base.py`）。
3. **Trajectory 仅元数据耦合**  
   RL loop 只读 `trajectory.metadata["runtime"]["rl"]`，不依赖任何 Hermes 类型 → fake / hermes / atropos runtime 共用同一 trainer。
4. **Opt-in 一切**  
   LoRA / Lagrangian / value head / reference policy / per-token advantage / 分布式 rollout / 多套 metrics 全部默认关闭。**v0.2 echo MVP 配置今天依然能跑通**。

---

## 3. 数据契约（核心 dataclass）

```python
# hermes_agentic_rl/core/types.py
@dataclass(slots=True)
class Trajectory:
    task_id: str
    prompt: str
    steps: list[RolloutStep]
    final_output: str | None
    finished_naturally: bool
    turns_used: int
    metadata: dict   # ↳ runtime.rl = {prompt_ids, response_ids, old_logprobs, temperature}

@dataclass(slots=True)
class RolloutStep:
    turn_index: int
    assistant_message: str | None
    tool_calls: list[dict]; tool_results: list[dict]
    reasoning: str | None

@dataclass(slots=True)
class RewardSummary:
    final_score: float
    components: list[RewardResult]    # 每个 reward 组件的分项明细
    metadata: dict

# hermes_agentic_rl/algos/base.py
@dataclass(slots=True)
class RolloutRecord:
    prompt_ids: list[int]
    response_ids: list[int]
    old_logprobs: list[float]   # ← rollout 时冻结，PPO ratio 的分母
    reward: float
    group_id: str               # ← GRPO 分组 key
    metadata: dict              # 可携带 _ppo_old_values / token_rewards / opd_hint
```

### 3.1 一次训练迭代的数据流

```
env.get_next_item()  →  agent_loop_factory(policy, seed)
                                    │ generate(prompt_ids, T, seed)
                                    ▼
        Trajectory{ steps[], metadata.runtime.rl = {prompt_ids, response_ids, old_logprobs} }
                                    │
                                    ▼
        RewardManager.evaluate(item, trajectory) → RewardSummary{final_score, components[]}
                                    │
                                    ▼
        RolloutRecord(prompt_ids, response_ids, old_logprobs, reward, group_id)
                                    │  × prompts_per_iter × group_size
                                    ▼
        Algo.compute_loss(policy, ref_policy, batch) → (loss, AlgoUpdateStats)
                                    │
                                    ▼
        AMP scale → backward → unscale → clip_grad → optim.step()
                                    │
                                    ▼
        CheckpointManager.save() + MetricsWriter(record)
```

---

## 4. 算法层 cheat sheet

### 4.1 七种算法对照

| 算法 | 文件 | 公式核心 | 适用场景 |
|---|---|---|---|
| **GRPO** | `algos/grpo.py` | `A_i = (R_i − μ_g) / (σ_g + ε)` | 默认主力，无 critic |
| **PPO** | `algos/ppo.py` | GAE + clipped value loss | 有稳定 critic 的场景 |
| **RLOO** | `algos/rloo.py` | `b_i = Σ_{j≠i} R_j / (G−1)`，`A_i = R_i − b_i` | G≥3 时方差更低，PRM ±1 兼容 |
| **OPD** | `algos/opd.py` | `A_t^OPD = log π_T(a_t \| s+hint) − log π_θ(a_t \| s)` | 有 next-state hint 的 token 级蒸馏 |
| **Hybrid** | `algos/hybrid.py` | `L = w_RL · L_GRPO + w_OPD · L_OPD` | 评估信号 + 指令信号融合 |
| **Factored** | `algos/factored.py` | `L = Σ_h w_h · L_clip(π_h_new, π_h_old, A)` | 5 头分解动作空间 |
| **OPD-TopK** | `algos/opd_topk.py` | per-token logp 做 hint Top-K 选择 | 减少 OPD 的判官调用 |

### 4.2 GRPO 的 5 种 advantage 归一化（重要）

| 模式 | 行为 | 来源 |
|---|---|---|
| `group` | `(r − μ_g)/(σ_g + ε)`，组内全等时返回 0 | DeepSeek-R1 标准 |
| `dapo` | 组内全等时**整组丢弃**（`return None`） | DAPO §3 (Dong 2025) |
| `batch` | 跨样本归一化 | TRL 兼容 |
| `whiten` | 白化 + clip | OpenAI-style |
| `none` | 原始 reward 直用 | OpenClaw-RL `--disable-rewards-normalization`（PRM 输出 ±1 时直接用） |

> 实现细节：见 `algos/common/advantage.py:6-62`。`group_normalize_advantage` 在 std<ε 时返回全 0；`dapo_group_advantage` 在 std<ε 时返回 `None`，调用方丢弃整组。

### 4.3 GRPO 工程关键点

1. **批量前向**：`policy.score_batch(prompt_ids_list, response_ids_list)` 一次得到 `[B, T_max]` logp + mask。HF GPT-2 上 **5–10× 加速**。
2. **非对称裁剪**：`clip_eps_high=0.28 > clip_eps=0.2`（OpenClaw-RL §3.1，缓解策略坍塌）。见 `algos/common/loss.py:63-66`。
3. **per-token advantage（REINFORCE++）**：`<answer>` 标签后 token 拿到全 reward，之前 token gamma 衰减。见 `algos/common/reinforce_pp.py:27-65`。
4. **3 种 KL 估计器**（`algos/common/kl.py`）：
   - `k1`：`mean(r)` — 直接 logp 差，可能为负
   - `k2`：`mean(0.5 * r²)` — 总是非负但有偏（其实是 ½χ²）
   - `k3`：`mean(exp(−r) − 1 + r)` — **TRL/DeepSeek/verl 推荐**，无偏非负低方差
5. **3 种 loss 聚合**：`mean_token` / `sum_token` / `dr_grpo`（Liu 2024，长度无偏）。

### 4.4 PPO 关键点

- 必须 `with_value_head=True` 的 backend
- 默认稀疏 token reward：`token_rewards[R_i-1] = reward`（终止位）；可通过 `metadata['token_rewards']` 注入稠密
- `_ppo_old_values` 由 `PPOTrainer._prepare_update_batch` 在 rollout 时冻结，让 `value_clip` 在 epoch 1 就生效（修了 v0.7 的 silent no-op bug）
- `compute_gae_batched` 向量化 GAE（`algos/common/gae.py`）
- 可选 advantage 白化 + clip（`whiten_advantage`、`advantage_clip=3.0`）

### 4.5 OPD（OpenClaw-RL §3.2）核心 idea

每个 next-state（用户回复 / 工具输出 / 测试结果）都隐式包含一个**指令组件**——告诉策略该如何修正。OPD 让 judge 把这个指令显式提取成文本 hint，然后构造一个 hint-conditioned 教师分布 π_T。Token 级优势：

```
A_t^OPD = log π_T(a_t | s + hint) − log π_θ(a_t | s)
```

每个 token 都有独立方向信号，比任何标量 reward 都更稠密。

---

## 5. Trainer 工程特性矩阵

`OnPolicyTrainer`（`trainers/on_policy.py`，~1800 行）是项目工程量最重的文件。所有特性 **opt-in**：

| 类别 | 特性 | 配置项 | 备注 |
|---|---|---|---|
| **学习率** | LR scheduler | `lr_schedule` | constant / linear / cosine / warmup_cosine |
| **优化** | Update epochs + minibatch | `update_epochs / minibatch_size` | PPO/GRPO 多 epoch 重训 |
| **trust region** | per-minibatch 早停 | `target_kl` | `approx_kl > 1.5 × target` 中止本 iter |
| **trust region** | Adaptive KL | `adaptive_kl` | InstructGPT A.2，跨 iter 调 β |
| **奖励** | RunningMeanStd 白化 | `normalize_reward` | `raw_reward` 进 metadata 用于日志 |
| **检查点** | 周期 + 最佳并存 | `checkpoint_every / save_best_checkpoint` | best 永不修剪 |
| **检查点** | Auto resume | `auto_resume / resume_from` | 完整 `{model, optim, rng, stats, kl_ctrl, rms}` |
| **早停** | 容忍轮数 | `early_stop_patience / early_stop_min_delta` | 与 best 联动 |
| **冷启动** | Bootstrap SFT | `bootstrap_sft_rounds` | RL 前先做几轮监督 |
| **抗遗忘** | Interleaved SFT | `interleave_sft_every` | 周期掺入 SFT 步 |
| **精度** | 混合精度 | `amp_dtype` | fp16 / bf16 / fp32 / auto |
| **吞吐** | 梯度累积 | `grad_accum_steps` | 模拟大 batch |
| **吞吐** | Batch generate | `batch_generate` | rollout 阶段批量采样 |
| **分布式** | FSDP / DDP | `distributed_strategy + fsdp_cpu_offload` | 训练 sharding |
| **分布式** | MP rollout pool | 注入 `rollout_pool` | 多进程 rollout，权重广播 |
| **分布式** | Ray rollout pool | `RayRolloutPool` | 可选 Ray，跨节点 |
| **加速** | vLLM rollout | `vllm_rollout_model` | 仅推理用 vLLM，每 N iter 同步权重 |
| **加速** | Flash Attention | `flash_attention` | HF + Tiny 后端可选 SDPA |
| **多轮** | Multi-turn credit | `multi_turn / multi_turn_credit` | 5 模式：`shared` / `terminal` / `discounted` / `judge` / `hybrid` |
| **课程** | 自动晋级 | `env.observe(reward)` | `CurriculumEnv` 移动均值阈值 |
| **约束** | Lagrangian | `lagrangian.penalty_term(loss)` | RCPO 双变量优化 |
| **塑形** | Reward shaping hook | `reward_shaping_fn` | 长度惩罚 / 格式奖励 |

### 5.1 Multi-turn credit 5 种模式（`trainers/multi_turn_credit.py`）

| 模式 | 每轮 reward 公式 |
|---|---|
| `shared` | 所有轮共享 `final_reward` |
| `terminal` | 仅最后一轮拿 `final_reward`，其余 0 |
| `discounted` | 按 `γ^(N−t−1)` 衰减分配 |
| `judge` | 仅用本地 judge 局部分（`final_weight=0`，`local_weight=1`）|
| `hybrid` | `0.7 × final + 0.3 × local`（默认权重） |

### 5.2 检查点（`trainers/checkpoint.py`）

- **原子写**：`tmp_<name>_xxx.partial` → `os.replace`，crash-safe
- **完整 state**：model / optimizer / RNG / stats / config / best_reward / running_stats / kl_ctrl_state
- **三个目录**：`checkpoints/`（周期）+ `checkpoints_best/`（最佳，永不修剪）+ `policy_iter_NNNN.pt`（兼容 legacy）

---

## 6. 奖励系统

### 6.1 Reward 组件目录（`rewards/`）

| 组件 | 信号源 | 用途 | 关键代码 |
|---|---|---|---|
| `OutcomeReward` | `trajectory.final_output` | 最终结果对错 | `outcome_reward.py` |
| `ToolcallReward` | `trajectory.steps[].tool_calls` | 工具调用结构有效性（name 缺失计数） | `toolcall_reward.py:23-60` |
| `FileSystemVerifierReward` | 文件系统状态 | 验证产出文件存在/内容匹配 | `filesystem_verifier_reward.py` |
| `NextTurnFeedbackReward` | 下一轮用户消息 | 识别"我需要纠正"等隐式反馈 | `next_turn_feedback.py` |
| `PRMComponent` | 步骤级 PRM 模型 | 每步打分（`split_steps`） | `prm.py` |
| **`NextStatePRM`** | next-state（OpenClaw-RL） | 多次 PRM 投票 → ±1/0；同时提取 hint 给 OPD | `next_state_prm.py` |
| `RewardModelComponent` | 训练后 RM head | 用 BC/DPO 数据训出来的 RM 直接打分 | `reward_model.py` |
| `LagrangianController` | 约束（如 turns ≤ K） | trainer 加惩罚项 | `lagrangian.py` |
| `weighted_sum` | 多组件 | 加权聚合 | `aggregate.py` |

### 6.2 Hermes-specific 奖励配置示例

`HermesReasoningTraceEnv` 同时构建 `tool_call_reward + text_reward`，**默认权重 0.75 : 0.25**——结构正确比内容相似更重要，符合 agent RL 直觉。

```yaml
# configs/hermes_reasoning_traces_grpo_smoke.yaml
environment:
  type: hermes_reasoning_traces
  reward_weight: 1.0
  # 内部默认 tool_call_reward_weight=0.75, text_reward_weight=0.25
```

### 6.3 Lagrangian 约束（RCPO-style）

```python
# 训练公式
max_π  E[r] − λ · ( E[c] − c_limit )
# 双变量更新
λ ← max(0, λ + lr_lambda · (mean_cost − c_limit))
```

典型用途：约束 `turns_used ≤ K`、`final_output_chars ≤ M`，避免策略学会"刷长度骗 reward"。

---

## 7. Hermes 运行时集成

### 7.1 多入口探测（`runtime/hermes_entrypoints.py`）

为兼容 Hermes 不同版本，按优先级查找：

1. `ai_agent`（最稳定，含完整 tool-calling loop）
2. `run_agent_loop`（旧版函数式入口）
3. `AgentLoop` 类
4. 其他 callable

`HermesRuntimeAdapter.build_agent_loop` 选中 `ai_agent` 时直接构造 `AIAgent(base_url, api_key, provider, model, enabled_toolsets, ...)`，包装为 `HermesAIAgentWrapper`。

### 7.2 多密钥回退（凭证不写 YAML）

```python
# runtime/hermes_adapter.py:_resolve_api_key
def _resolve_api_key(runtime_cfg):
    1. literal `runtime.api_key`            # 测试用
    2. `runtime.api_key_envs` 列表顺序遍历   # 推荐
    3. `runtime.api_key_env` 单字段          # 兼容旧字段
```

```yaml
runtime:
  api_key_envs: [NEWAPI_API_KEY, LKEAP_API_KEY, OPENAI_API_KEY]
```

> ✅ 满足 `security_rules` 的"无硬编码凭证"要求；同时支持团队共享配置 + 个人 env 覆盖。

### 7.3 仓库路径解析（`integrations/hermes_repo.py` & `atropos_repo.py`）

| 项目 | 环境变量 | 默认顺序 |
|---|---|---|
| hermes-agent | `HERMES_AGENT_REPO` | `subprojects/hermes-agent` → `hermes-agent` → `vendor/hermes-agent` |
| atropos | `ATROPOS_REPO` | `subprojects/atropos` → `subprojects/hermes-agent/atropos` → `atropos` → `vendor/atropos` |
| tinker-atropos | `TINKER_ATROPOS_REPO` | 同上 |

`*-preflight` CLI 输出 `{repo_source, missing[]}`，CI/用户快速定位 submodule 配置：

```bash
$ python -m hermes_agentic_rl.cli.main hermes-preflight
{
  "repo_source": "subproject",
  "repo_path": "/abs/path/to/subprojects/hermes-agent",
  "missing": []
}
```

---

## 8. 评估系统与促晋闸门

### 8.1 三种评估命令

| 命令 | 用途 | 退出码 |
|---|---|---|
| `eval-rl` | 普通 held-out 评估，写 `eval_summary.json` + `capability_report.md` + `promotion.md` | `0` 总是成功 |
| **`eval-gate`** | 强制 `promotion_gate.fail_on_hold=true` | `3` = 拒绝晋级 |
| `train` 内置闸门 | `--min-nonzero-ratio` / `--min-verifier-pass-ratio` | `3`/`4` = 训练质量不达标 |

### 8.2 关键设计：分组保持（防泄漏）

```python
# eval/rl_eval.py:select_items_by_group
select_items_by_group(items, split, group_key="source_trace_id", val_ratio, test_ratio, seed)
```

**同一条 trace 的所有轮次必须落在同一个 split**，避免训练用了 turn-3、评估用了 turn-5 的同 trace 泄漏。

### 8.3 `paired_welch_t`（`eval/ab_test.py:31-55`）

纯 stdlib 实现的配对 Welch t-test：

```python
@dataclass class ABResult:
    n: int; mean_diff: float; t: float; approx_p: float
    winner: Literal["candidate", "baseline", "tie"]
```

- 用标准正态 CDF 近似 p（`n ≳ 30` 时有效）
- `α=0.05` 默认显著性

### 8.4 Capability Axes（4 大能力维度）

```python
# eval/capability_axes.py
DEFAULT_CAPABILITY_AXES = (
    task_success           ← mean_reward 0.6 + success_rate 0.4
    tool_use_reliability   ← parse_ok 0.25 + name_match 0.25 +
                            argument_key_overlap 0.25 + value_similarity 0.25
    interaction_control    ← finished_naturally_rate 0.5 +
                            success_metric_found_rate 0.5
    self_evolution_signal  ← success_score_mean 0.5 + mean_reward 0.5
)
```

输出 markdown 报告，比单一 reward 更利于决策"接下来优化什么"。

### 8.5 Promotion Readout

`eval-rl` 自动选 best non-baseline candidate，比较 reward/success-rate Δ，输出 `promote / hold` 推荐到 `promotion.md`。`fail_on_hold=true` 时 CI 直接阻断。

---

## 9. Online Cycle 闭环

### 9.1 一次 cycle 的 5 步

```
1. Hermes rollout         HermesRuntimeAdapter 跑真实 agent，session 写入 sidecar
       │
       ▼
2. Sidecar 异步落盘        后台线程 + queue.Queue，flush 写
                          sessions.jsonl + replay.jsonl
       │
       ▼
3. Worker 训练            可选 bc / dpo / rm，从 replay 读取，训出 policy.pt
       │
       ▼
4. Self-evolution 导出     session_eval_export 输出
                          train.jsonl / val.jsonl / holdout.jsonl + manifest.json
       │
       ▼
5. Metrics                worker_metrics.jsonl + W&B
```

### 9.2 Sidecar 设计要点（`collectors/sidecar.py`）

| 特性 | 实现 |
|---|---|
| 异步线程 | `threading.Thread(target=_worker, daemon=True)` |
| 队列 | `queue.Queue`（默认无界，`queue_maxsize` 可配） |
| Flush | `flush_interval_sec=0.5` + `max_batch_size=16` 双触发 |
| 失败处理 | `flush_errors` 计数，**永不阻塞 rollout** |
| 背压 | `drop_when_full=True` 可选丢弃策略 |
| 完整统计 | `SidecarStats` TypedDict 11 个字段 |

### 9.3 Replay 质量门控（可选）

`session_replay` 阶段可启用：
- `min_prompt_tokens / min_response_tokens`
- `dedupe_within_scan`
- `quarantine_path`：低质量样本隔离
- `quality_report_path`：JSON 报告

---

## 10. 配置体系（29 份 YAML）

按"应用场景"分类：

| 类别 | 数量 | 代表配置 | 用途 |
|---|---|---|---|
| **Smoke / MVP** | 4 | `echo_grpo_mvp.yaml`、`echo_ppo_mvp.yaml` | CI 闭环验证 |
| **真实 HF 数据 GRPO** | 2 | `hermes_reasoning_traces_grpo*.yaml` | `lambda/hermes-agent-reasoning-traces` |
| **本地 Parquet on MPS** | 4 | `*_parquet_mps*.yaml` | 苹果 MPS，含 filtered / curriculum / stage2 |
| **Held-out 评估** | 2 | `*_eval_rl*.yaml` | baseline vs ckpt |
| **Self-evolution** | 2 | `self_evolution_batch.yaml`、`session_eval_export_hermes.yaml` | 方向化批量导出 |
| **Online Cycle** | 1 | `hermes_online_cycle.yaml` | 端到端闭环 |
| **Worker 训练** | 3 | `session_train_worker.yaml`、`session_dpo_worker.yaml`、`session_rm_worker.yaml` | BC/DPO/RM |
| **Offline** | 3 | `offline_bc_mvp.yaml`、`offline_dpo_mvp.yaml`、`offline_rm_mvp.yaml` | 静态数据训练 |
| **课程 / 多轮** | 2 | `curriculum_grpo.yaml`、`sim_tool_grpo_multiturn.yaml` | 高级用法 |
| **Terminal 行动空间** | 4 | `terminal_grpo*.yaml`、`*_terminal_command_stage2.yaml` | 命令行动作分阶段课程 |
| **其他** | 2 | `code_fix_grpo_v05.yaml`、`letter_counting_grpo_v05.yaml` | 早期实验 |

### 10.1 配置范式（7 个顶层 section）

```yaml
runtime:        # integration / provider / api_keys / sidecar
environment:    # type / dataset / reward 配置
backend:        # tiny / hf 选择与超参
agent_loop:     # policy / multi_turn
train_rl:       # n_iters / lr / clip_eps / 全部 trainer 选项
metrics:        # jsonl / tensorboard / wandb / dashboard
dashboard:      # 可选 Live Dashboard
```

---

## 11. 可观测性栈

### 11.1 四路 fan-out（`monitor/writers.py`）

| Writer | 配置 | 行为 |
|---|---|---|
| **stdout** | `metrics.stdout` | 一行/iter，主键序 + 数值 extras |
| **JSONL** | `metrics.jsonl` | `metrics.jsonl` 文件，结构化每行 |
| **TensorBoard** | `metrics.tensorboard` | `tb/` 目录 |
| **W&B** | `metrics.wandb` | name/project/tags/mode 完整 |
| **Live Dashboard** | `dashboard.enabled` | 纯 stdlib HTTP server，零外部依赖 |

`MultiMetricsWriter` 一次 callable 调用 fan-out 到所有目的地。

### 11.2 一个 iter 至少含 30+ 个结构化指标

**核心指标**：
- `iter` / `algo` / `loss` / `policy_loss` / `value_loss` / `kl` / `clip_frac` / `entropy`
- `mean_reward` / `mean_advantage` / `approx_kl` / `ratio_mean`
- `grad_norm` / `param_norm` / `grad_clip_triggered` / `optimizer_step_applied` / `amp_scale` / `lr`

**Reward 组件分解**（每个组件）：
- `mean / min / max / std / weight_mean`
- 组件内部 metadata 数值字段（`tool_call_parse_ok`、`tool_name_match`、`argument_value_similarity`...）

**Rollout 元信息**（带 series_stats）：
- `prompt_tokens` / `response_tokens` / `turns_used` / `tool_calls_count` / `tool_results_count`
- `rollout_temperature` / `final_output_chars`
- `finished_naturally_rate`

**Multi-turn 时**：
- `turn_credit_reward_mean` / `turn_credit_local_component_mean` / `turn_credit_final_component_mean`
- `n_turn_records` / `turn_index` 分布

**Curriculum**：`env_snapshot.current_level / promotions / demotions`

---

## 12. 横向对比（vs verl / TRL / OpenRLHF）

| 维度 | hermes-agentic-rl | verl (Bytedance) | TRL (HF) | OpenRLHF |
|---|---|---|---|---|
| **算法覆盖** | GRPO/PPO/RLOO/OPD/Hybrid/Factored | GRPO/PPO/REINFORCE | PPO/DPO/GRPO/RLOO | PPO/DPO/GRPO/Iterative DPO |
| **Backend 协议化** | ✅ Protocol 自由替换 | ✅ HybridFlow（更彻底） | 锁 HF | 锁 HF |
| **真实 agent runtime** | ✅ Hermes 多入口适配 | ❌ 仅 generate | ❌ 仅 generate | ❌ 仅 generate |
| **Tool-call reward 组件** | ✅ 内置多个 | ❌ | ❌ | ❌ |
| **Eval-as-gate** | ✅ exit code 3 | ❌ | ❌ | ❌ |
| **Self-evolution export** | ✅ task_input/expected_behavior JSONL | ❌ | ❌ | ❌ |
| **OpenClaw-RL idea 移植** | ✅ NextStatePRM/OPD/Hybrid | ❌ | ❌ | ❌ |
| **课程学习** | ✅ CurriculumEnv 自动晋级 | ⚠️ 需手写 | ❌ | ❌ |
| **Lagrangian 约束** | ✅ RCPO 内置 | ❌ | ❌ | ❌ |
| **vLLM rollout** | ✅ 同步权重 | ✅✅ 更深集成 | ⚠️ 实验性 | ✅ |
| **FSDP / DDP** | ✅ 接入 | ✅✅ 主力 | ✅ | ✅ |
| **CPU smoke 闭环** | ✅ Tiny backend 30K params | ❌ | ⚠️ 慢 | ❌ |
| **Live Dashboard** | ✅ 纯 stdlib HTTP | ❌ | ❌ | ❌ |
| **代码体量** | ~135 模块（中型） | 大型 | 大型（HF 一部分） | 大型 |

**定位差异**：

- **verl** 偏 ML infra，HybridFlow 优雅，但**没有 agent runtime**
- **TRL** 通用 RLHF，**与 HF 紧耦合**，缺 agent 概念
- **OpenRLHF** 大规模 RLHF，**生产 LLM 训练偏向**
- **hermes-agentic-rl** 是这四者中**唯一面向 agent 行为分布**的，trade-off 是 backbone scale 不如它们

---

## 13. 工程基线与质量门

### 13.1 工具链（`pyproject.toml`）

| 工具 | 版本 | 作用 |
|---|---|---|
| **ruff** | `0.6.9` | E/W/F/I/B/UP/SIM/RUF；line-length=100；formatter 替代 black+isort |
| **mypy** | `1.11.2` | full-package 阻塞；`disallow_untyped_defs=false`（渐进式严格化） |
| **pytest + cov** | 7.4+ / 6.0+ | `fail_under=45`，分支覆盖 |
| **pre-commit** | 3.7+ | git 钩子 |
| **pip-audit** | 2.8+ | 依赖审计 |
| **Sphinx + MyST** | 7.3+ / 3.0+ | 文档构建（`docs/sphinx/`） |
| **uv** | 用于 lock | `requirements-lock.txt` |

### 13.2 CI 检查（README §Verification）

```bash
ruff check                                         # lint
mypy --follow-imports=skip hermes_agentic_rl       # type
pytest --cov --cov-report=xml                      # coverage ≥ 45%
python scripts/ci_eval_gate_smoke.py               # eval-gate 退出码 3 验证
sphinx-build -W docs/sphinx                        # 文档零警告
uv pip compile + pip-audit                         # 依赖安全
```

### 13.3 安全合规（对照 `security_rules`）

| 规则 | 项目状态 |
|---|---|
| SQL 注入 | ✅ 项目无 DB 层，N/A |
| 凭证硬编码 | ✅ `api_key_envs` 顺序回退；不写 YAML |
| 参数化查询 | ✅ N/A |
| 敏感日志 | ✅ Sidecar 不打印 API key |
| LoRA 推理零开销 | ✅ `merge_into_base()` 折叠到 `base.weight` |

---

## 14. 技术债务清单

| 项 | 位置 | 严重度 | 备注 |
|---|---|---|---|
| ruff 多条规则禁用 | `pyproject.toml:129-144` | 中 | `B008 / B023 / B007 / SIM105 / E741` 等部分意图、部分历史 |
| mypy 仍允许 untyped defs | `pyproject.toml:165` | 中 | 注释明确"渐进式"，但近期未推进 |
| 直接修改 `sys.modules` | `runtime/hermes_adapter.py:46-48` | 中 | 多入口必要 hack；建议加锁/守护更严 |
| `HermesAIAgentWrapper` 多 try/except 兜底版本差异 | `hermes_adapter.py:142` | 低 | 现实必要，无解 |
| Interleaved SFT 与 RL 共用 optimizer | `on_policy.py:1380` | 低 | LR 切换用 try/finally 恢复，语义清晰但脆弱 |
| Tiny backend 不能学到真实能力 | `backends/tiny.py` | 低 | 明确"smoke only"；已在 README 强调 |
| `outputs/` 与 `output/` 共存 | 根目录 | 低 | 命名冲突，应统一 |
| Atropos 依赖 zip 包 + submodule 双路径 | `atropos-main.zip` + `subprojects/` | 低 | 历史兼容 |

---

## 15. 改进建议（按优先级）

### P0 — 决定项目能否真正训出能力

1. **从 Tiny → HF + LoRA**  
   切到 Qwen2.5-3B/7B + LoRA（rank 8 / 16），让 GRPO 真正影响有容量的策略。`peft/lora.py` 已就绪，缺 e2e 配置示例。
2. **扩充 held-out benchmark 体量**  
   现有 `lambda/hermes-agent-reasoning-traces` smoke 太小，`capability_axes` 4 维趋势线方差大。建议 ≥500 个 trace、覆盖 8+ 工具类型。

### P1 — 直接提升训练效果

3. **Reward shaping 强化**  
   - JSON validity 严格分（用 jsonschema）
   - tool_name 与 ground truth 编辑距离归一
   - argument_value_similarity 改 token-level F1
   - multi-turn credit assignment 引入 `judge` 模式默认配置（目前 `shared` 浪费稀疏信号）
4. **课程梯度标准化**  
   把"格式 → 简单工具 → 复杂工具 → 多轮"做成 4 档 `CurriculumEnv` 默认配置，每档有自身 promote_threshold。
5. **OPD/Hybrid 真实数据验证**  
   实现已 ready，但缺 Hermes traces 上的对照实验。建议用 `terminal_command_stage2` 做 baseline，跑 GRPO vs Hybrid 配对 A/B。

### P2 — 工程基础

6. **批量推理对比基准**  
   默认开 `batch_generate`，HF backend 上跑 `score_batch` 实测对比；MPS profile。
7. **分布式 e2e 配置示例**  
   `MPRolloutPool` 已就绪但缺 `configs/distributed_grpo_mvp.yaml`，建议补上 + 集成测试。
8. **更严的 mypy**  
   把 `algos/` 和 `core/` 先拉到 `disallow_untyped_defs=true`，逐步推进到全包。

### P3 — 长期演进

9. **GSPO / DAPO / RFT 跟进**  
   目前 `dapo` 已是 advantage 模式，但 GSPO（序列级 group baseline）还没。
10. **AGI-style episodic memory** 与 `mem_op / mem_slot` 因子化动作空间结合。

---

## 16. 复用价值评估（给读者）

### 16.1 你属于哪类读者？

| 你想做的事 | 这个项目的价值 |
|---|---|
| **学习 GRPO/PPO 工程实现** | ⭐⭐⭐⭐⭐ `algos/grpo.py` + `algos/common/` 是少见的批量化 + 5 种 advantage 归一化的参考实现 |
| **学习 OpenClaw-RL 思想（OPD/Hybrid）** | ⭐⭐⭐⭐⭐ 是为数不多的工程化移植 |
| **训练自己 agent 的 RL** | ⭐⭐⭐⭐ Hermes-specific，但 Env / Reward / Runtime 协议都可换 |
| **通用 LLM RLHF** | ⭐⭐ 可以但偏题，TRL 更直接 |
| **研究 multi-turn credit assignment** | ⭐⭐⭐⭐ 5 种模式 + 各种聚合统计 |
| **工业级 RL 大规模训练** | ⭐⭐ vLLM / FSDP 接入了但不是主力，verl 更适合 |
| **CI/CD 把 RL 评估自动化** | ⭐⭐⭐⭐⭐ `eval-gate` exit code 3 是真正可工程化的设计 |
| **学习 Sidecar + Self-evolution 模式** | ⭐⭐⭐⭐⭐ 异步 sidecar + replay 闭环少见 |

### 16.2 直接可拷贝的高质量模块

| 模块 | 复用价值 | 原因 |
|---|---|---|
| `algos/common/{loss,kl,gae,advantage}.py` | ⭐⭐⭐⭐⭐ | 纯 torch + 完整文档 + 全 batch 化，单元测试友好 |
| `eval/ab_test.py` | ⭐⭐⭐⭐⭐ | 60 行 stdlib 实现 paired Welch t，无 scipy 依赖 |
| `trainers/checkpoint.py` | ⭐⭐⭐⭐ | 原子写 + 完整 state；跨 RL 项目通用 |
| `monitor/writers.py` + `dashboard.py` | ⭐⭐⭐⭐ | stdlib HTTP dashboard 极简实用 |
| `peft/lora.py` | ⭐⭐⭐⭐ | substring-match target + idempotent merge |
| `eval/capability_axes.py` | ⭐⭐⭐⭐ | 把指标按能力维度组织，可移植到任何评估系统 |
| `runtime/hermes_entrypoints.py` | ⭐⭐⭐ | 多入口探测模式，外部 SDK 集成可借鉴 |

---

## 17. 一图速览 & 结论

### 17.1 总览图

```
                    ┌─────────────────────────────────────┐
                    │       Hermes-agent (执行层)          │
                    │  真实 tool calls / terminal / files  │
                    └────────────┬────────────────────────┘
                                 │ session traces
                                 ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │                  hermes-agentic-rl (学习层)                      │
   │                                                                 │
   │  ┌─ Online-Cycle 流水线 ────────────────────────────────────┐  │
   │  │ Sidecar (异步落盘) → Replay (质量门控)                    │  │
   │  │              → Worker (BC/DPO/RM) → Self-Evo (JSONL)      │  │
   │  └───────────────────────────────────────────────────────────┘  │
   │                                                                 │
   │  ┌─ Train-RL 流水线 ────────────────────────────────────────┐  │
   │  │ Env → AgentLoop → Trajectory → Rewards → RolloutBatch    │  │
   │  │   → Algo (GRPO/PPO/RLOO/OPD/Hybrid/Factored)             │  │
   │  │   → AMP/grad_accum → Optimizer step → CheckpointManager  │  │
   │  └───────────────────────────────────────────────────────────┘  │
   │                                                                 │
   │  ┌─ Eval / Eval-Gate 闸门 ──────────────────────────────────┐  │
   │  │ 分组 split → 多 policy 并行评估 → capability_axes →      │  │
   │  │   paired Welch t-test → promotion_readout → exit code    │  │
   │  └───────────────────────────────────────────────────────────┘  │
   └─────────────────────────────────────────────────────────────────┘
                                 ▼
                    ┌─────────────────────────────────────┐
                    │ Observability                       │
                    │  W&B + TensorBoard + JSONL + Live   │
                    │  Dashboard (stdlib HTTP)            │
                    └─────────────────────────────────────┘
```

### 17.2 核心结论

**hermes-agentic-rl 是一个工程化程度极高、研究新点完整覆盖、但单体能力上限受限于默认 Tiny backend 的 agentic-RL 框架。**

- ✅ **闭环完整**：从 rollout 到 promotion，每环都有 CLI 入口和退出码语义
- ✅ **算法前沿**：GRPO + RLOO + OPD + Hybrid + Factored 几乎覆盖 2024–2026 agentic RL 主线
- ✅ **可审计**：W&B/TB/JSONL/dashboard + capability_report.md + promotion.md 决策有据可循
- ✅ **成本梯度**：Tiny CPU smoke → MPS local parquet → HF + LoRA → vLLM + FSDP，逐级加码
- ⚠️ **能力天花板**：默认 Tiny backbone 仅供 smoke；需切到 HF + LoRA 才能产出实战能力
- ⚠️ **agent 绑定**：与 Hermes-agent 强绑定；通用 RLHF 选择 TRL/verl 更合适

### 17.3 三句话推荐

1. 如果你做 **agent RL 研究**，这是把 OpenClaw-RL（OPD / NextStatePRM / Hybrid）系统工程化的少数实现，值得通读。
2. 如果你做 **生产级 RL 流水线**，可以把它的 `eval-gate / sidecar / capability_axes / checkpoint` 几个组件直接拆出来复用——它们的设计独立性很高。
3. 如果你做 **通用 RLHF 模型训练**，更建议看 verl/TRL/OpenRLHF；hermes-agentic-rl 强行用会绕远路。

---

## 附录 A：项目命令速查

```bash
# 准备
git submodule update --init
python -m pip install -e '.[rl,data,metrics]'
python -m hermes_agentic_rl.cli.main hermes-preflight
python -m hermes_agentic_rl.cli.main atropos-preflight

# 训练
python -m hermes_agentic_rl.cli.main train-rl     --config <yaml> --output <dir>
python -m hermes_agentic_rl.cli.main online-cycle --config <yaml> --once --limit 1
python -m hermes_agentic_rl.cli.main self-evolution-batch --config <yaml>

# 评估
python -m hermes_agentic_rl.cli.main eval-rl   --config <yaml>
python -m hermes_agentic_rl.cli.main eval-gate --config <yaml>   # exit code 3 阻断

# Worker / Replay
python -m hermes_agentic_rl.cli.main session-replay         --config <yaml>
python -m hermes_agentic_rl.cli.main session-train-worker   --config <yaml> --once
python -m hermes_agentic_rl.cli.main session-eval-export    --config <yaml>

# 调试
python -m hermes_agentic_rl.cli.main rollout --config <yaml> --output trajectory.json
python -m hermes_agentic_rl.cli.main --version
```

## 附录 B：关键环境变量

```bash
export HERMES_AGENT_REPO=/path/to/hermes-agent       # 覆盖 Hermes 仓库路径
export ATROPOS_REPO=/path/to/atropos                  # 覆盖 Atropos 仓库路径
export TINKER_ATROPOS_REPO=/path/to/tinker-atropos    # 覆盖 Tinker-Atropos
export NEWAPI_API_KEY=...                             # online-cycle 默认密钥
export LKEAP_API_KEY=...                              # 备用
export OPENAI_API_KEY=...                             # 最后回退
export WANDB_API_KEY=...                              # W&B（或 wandb login）
export PYTORCH_ENABLE_MPS_FALLBACK=1                  # macOS MPS 训练
export TERMINAL_CWD=/path/to/workdir                  # train CLI workdir 隔离
```

## 附录 C：阅读源码的推荐路径

| 顺序 | 文件 | 理由 |
|---|---|---|
| 1 | `core/types.py` | 所有数据契约的源头 |
| 2 | `algos/base.py` | RolloutRecord / RolloutBatch / AlgoUpdateStats / BaseAlgo |
| 3 | `algos/common/loss.py` | 批量 PPO clipped surrogate，所有算法的引擎 |
| 4 | `algos/grpo.py` | 主力算法实现 |
| 5 | `trainers/on_policy.py` | 共享 skeleton（核心 1800 行，工程价值最高） |
| 6 | `cli/train_rl.py` | YAML → trainer 的工厂逻辑 |
| 7 | `runtime/hermes_adapter.py` | Hermes 集成的所有 hack 都在这里 |
| 8 | `eval/rl_eval.py` | 评估闸门的完整实现 |
| 9 | `cli/online_cycle_cli.py` | online-cycle 的 5 步编排 |
| 10 | `collectors/sidecar.py` | 异步落盘的并发设计 |
